import 'package:flutter/material.dart';

class CallDoctorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Call Doctor'),
      ),
      body: Center(
        child: Text('Call Doctor Page'),
      ),
    );
  }
}
