import 'package:flutter/material.dart';

class MakeAppointmentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Make Appointment'),
      ),
      body: Center(
        child: Text('Make Appointment Page'),
      ),
    );
  }
}